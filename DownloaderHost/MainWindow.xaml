<Window x:Class="DownloaderHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DownloaderHost"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="300"></RowDefinition>
            <RowDefinition Height="300"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Download Directory" Margin="0 0 5 0"/>
            <TextBox Text="{Binding Path=DownloadPath, Mode=TwoWay}" />
        </StackPanel>
        <ListBox ItemsSource="{Binding DownloadsInProgress}" 
                 Grid.Row="1"/>
        <ListBox ItemsSource="{Binding FinishedDownloads}"
                 Grid.Row="2"/>
    </Grid>
</Window>
